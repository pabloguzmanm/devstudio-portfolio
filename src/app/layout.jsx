import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "DevStudio - Desarrollo Web y Soluciones Digitales",
  description: "Impulsa tu presencia en línea con DevStudio: desarrollo web personalizado, diseño UX/UI, comercio electrónico y marketing digital para llevar tu negocio al siguiente nivel.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
